<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp"
    android:layout_marginHorizontal="8dp"
    android:background="@color/white"
    android:foreground="?android:attr/selectableItemBackground">

    <!-- Removed ShapeableImageView -->

    <TextView
        android:id="@+id/tvriver"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:text="Elliðaá"
        android:textSize="16dp"
        android:textStyle="bold"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="32dp"
        android:layout_marginTop="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tilsolu"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="For Trade"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvriver"
        app:layout_constraintStart_toStartOf="@id/tvriver"
        app:layout_constraintEnd_toEndOf="@id/tvriver" />

    <TextView
        android:id="@+id/tvownerid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Owner ID"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tilsolu"
        app:layout_constraintStart_toStartOf="@id/tilsolu"
        app:layout_constraintEnd_toEndOf="@id/tilsolu" />

    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_marginTop="8dp"
        android:background="@color/underline"
        app:layout_constraintTop_toBottomOf="@id/tvownerid"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>